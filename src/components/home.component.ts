import {Component, Input, OnInit, AfterViewInit} from '@angular/core'
import {Router} from '@angular/router'


@Component({
    selector: 'home',
    templateUrl: '/static/templates/components/home.html',
})
export class HomeComponent {
    
}
